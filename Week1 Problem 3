final = s[0]
temp = s[0]
for i in range(1,len(s)):
    if s[i] >= s[i-1]:
        temp = temp + s[i]
        if len(temp) > len(final):
            final = temp
    else:
        temp = s[i]
print('Longest substring in alphabetical order is: '+final)
